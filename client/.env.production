# Same registrable domain as this client, so the SameSite=Lax session cookie is sent.
# mail-proxy.qwix.ca reaches the same box but is cross-site — the browser sends no cookie there.
VITE_API_URL=https://api.qwixmail.com
